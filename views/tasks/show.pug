<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h1 Show
        h1= task.description
        p= task.createdAt
        p
            strong Autor: &nbsp;
            span= task.user.email
        //ul
            each val in task.categories
                li= val
        form(action=`/tasks/${task.id}?_method=DELETE` method="POST")
            input(type="submit", value="Eliminar")
