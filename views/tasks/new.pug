<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        form(action="/tasks" method="POST")
            textarea(name="description", cols="30", rows="10" placeholder="Cola aqui tu pendiente a realizar")
            input(type="submit", value="Guardar")