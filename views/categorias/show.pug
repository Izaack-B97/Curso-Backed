<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h1= categoria.title
        p= categoria.color
  
        form(action=`/categories/${categoria.id}?_method=DELETE` method="POST")
            input(type="submit", value="Eliminar")
