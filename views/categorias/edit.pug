<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h1 Editar
        form(action=`/categories/${categoria.id}?_method=PUT` method="POST")
            input(type="text" name="title" placeholder="Title" value=categoria.title)
            input(type="text" name="color" placeholder="Color" value=categoria.color)
            input(type="submit", value="Guardar")